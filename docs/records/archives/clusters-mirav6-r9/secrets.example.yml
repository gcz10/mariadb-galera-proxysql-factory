---
# secrets.example.yml — SZABLON, nie zawiera prawdziwych sekretów.
# Prawdziwe sekrety trafiają do secret backendu (ansible_vault / hashicorp_vault / external).
# Nigdy nie commituj prawdziwych haseł, kluczy ani certów.

mariadb:
  root_password: "VAULT:replace-me"
  sst_user_password: "VAULT:replace-me"
  app_user_password: "VAULT:replace-me"
  monitor_user_password: "VAULT:replace-me"

proxysql:
  admin_password: "VAULT:replace-me"
  monitor_password: "VAULT:replace-me"

galera:
  wsrep_sst_auth: "VAULT:replace-me"

keepalived:
  vrrp_pass: "VAULT:replace-me"

backup:
  encryption_key: "VAULT:replace-me"

smb:
  username: "VAULT:replace-me"
  password: "VAULT:replace-me"
  share: "VAULT:replace-me"
