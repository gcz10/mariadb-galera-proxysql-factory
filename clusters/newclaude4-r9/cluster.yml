---
# newclaude4-r9 — klaster Galera na Rocky Linux 9 (Proxmox, real VM).
#
# Adresacja: n4g1-3 na .170-.172 (VMID 9440-9442), restore n4r1 .173 (9443).
#
# WARSTWA WSPOLNA (terraform/shared/): ProxySQL fcp1/fcp2 .131/.132, VIP .133,
# infra .130 (PMM + MinIO). TA SAMA para ProxySQL obsluguje finalclaude-r10.
#
# ROZLACZNOSC NA WSPOLNYM ProxySQL:
#   hostgroup_base: 310 (writer 310, backup 320, reader 330, offline 340)
#   finalclaude-r10 uzywa domyslnych 10/20/30/40
#   app_user: app_user_n4
#   wpis w mysql_users jest globalny, a jego default_hostgroup wybiera klaster
#   Pilnuje tego tests/validation/probe-proxysql-tenancy.py.
#
# TLS jest WLACZONY na tym klastrze z rozlacznym CA per klaster:
# /etc/mysql/tls/<klaster>/ca.pem — sciezka per klaster i przechodnia dla
# uzytkownika `proxysql`. Pilnuje tego probe-proxysql-tenancy.py.

cluster:
  name: "newclaude4-r9"
  environment: "laboratory"
  profile: "laboratory"
  automation_release: "0.1.0"

platform:
  virtualization: "proxmox_kvm"
  rocky_linux_major: 9

versions:
  policy: "candidate"
  lock_file: "versions/versions.lock.yml"

galera:
  cluster_name: "n4_galera"
  nodes_expected: 3

proxysql:
  nodes_expected: 2
  max_writers: 1
  read_write_split_enabled: false
  hostgroup_base: 310
  app_user: "app_user_n4"
  role: "consumer"
  endpoint:
    type: "keepalived_vip"
    address: "192.168.1.133"   # VIP warstwy wspolnej — ten sam co finalclaude-r10
    port: 6033

tls:
  mode: "full"
  socket_dynamic: false
  certificate_source: "file"
  ca_reference: "tests/lab/tls/n4/ca.pem"
  certificate_reference: "tests/lab/tls/n4/server-cert.pem"
  private_key_reference: "tests/lab/tls/n4/server-key.pem"
  require_secure_transport: false

network:
  application_cidrs: ["192.168.1.0/24"]
  database_cluster_cidrs: ["192.168.1.0/24"]
  administration_cidrs: ["192.168.1.0/24"]
  monitoring_cidrs: ["192.168.1.130/32"]

secrets:
  backend: "ansible_vault"

storage:
  data_directory: "/var/lib/mysql"
  backup_staging_directory: "/var/tmp/mariadb-backup"
  filesystem: "xfs"
  expected_database_size_gb: "unknown"
  expected_growth_gb_per_month: "unknown"
  available_iops: "unknown"

workload:
  peak_qps: "unknown"
  peak_connections: "unknown"
  read_write_ratio: "unknown"
  largest_transaction_mb: "unknown"
  largest_table_gb: "unknown"
  expected_write_latency_ms: "unknown"

availability:
  rpo: "0"
  rto_node_failure: "2m"
  rto_full_cluster_failure: "30m"
  maintenance_window: "weekend"
  allowed_service_interruption: "2m"

backup:
  enabled: true
  destination: "s3"
  full_backup_schedule: "30 2 * * *"
  incremental_backup_schedule: "disabled"
  freshness_sla_hours: 26
  retention_days: 14
  encryption_enabled: true
  immutable_or_offsite_copy: true
  restore_test_schedule: "0 5 * * 0"
  scheduler:
    mode: "cron"
    host: "n4g1"
    timezone: "UTC"
  s3:
    endpoint: "192.168.1.130:9000"
    bucket: "n4-galera-backups"
    secure: false

monitoring:
  pmm:
    server_url: "https://192.168.1.130"
    agent_id: "pmm-server"
    cluster_name: "n4-galera"
    validate_certs: false
    credentials_revision: 4
  agent_groups: ["galera"]
  qan_source: "slowlog"
  system: "pmm"
  alerts:
    email: "ops@claude.local"
  log_destination: "journald"

mariadb_tuning:
  innodb_buffer_pool_size: "768M"
  innodb_log_file_size: "256M"
  max_connections: 100
  innodb_flush_log_at_trx_commit: 0
  gcache_size: "256M"
  slow_query_log: "ON"
  long_query_time: 0.1
