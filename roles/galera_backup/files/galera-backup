#!/usr/bin/env python3
"""galera-backup — Cluster-aware physical Galera backup & restore utility.

Cienki wrapper. Cala logika mieszka w pakiecie `galera_backup`, ktory lezy OBOK
tego pliku na hoscie (/opt/galera-backup/) — stad bootstrap sciezki ponizej.
Rola kopiuje oba, patrz roles/galera_backup/tasks/main.yml.

Rdzen jest w `galera_backup.pipeline`, nie tutaj, i to NIE jest podzial
kosmetyczny: testy podmieniaja szesc symboli przez `patch.object(mod, ...)`,
wiec ich wywolujacy musza rozwiazywac te nazwy w tej samej przestrzeni nazw.
`galera_backup_testlib` laduje wlasnie `pipeline` z tego powodu.
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from galera_backup.pipeline import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
